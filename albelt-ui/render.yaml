services:
  - type: web
    runtime: static
    name: gestion-stock-albelt-1
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
